/// <reference types="vite/client" />
declare const __APP_NAME__: string;
// Instante do `vite build` (ISO 8601). Injetado por vite.config.ts `define`.
// Reflete quando o bundle da UI foi gerado (build-dist.yml) = o que está no cliente.
declare const __BUILD_TIME__: string;
