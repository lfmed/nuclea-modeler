bundle:
  name: nuclea-modeler

sync:
  include:
    - .build

artifacts:
  default:
    build: apx build

resources:
  apps:
    nuclea-modeler-app:
      name: "nuclea-modeler"
      description: "nuclea-modeler created with apx"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
